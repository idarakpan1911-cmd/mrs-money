"use client";
import Link from "next/link"; import { useState } from "react";
export function AuthForm({ signup=false }: {signup?:boolean}) { const [done,setDone]=useState(false); return <form onSubmit={e=>{e.preventDefault();setDone(true)}} className="card mx-auto max-w-md p-7"><p className="eyebrow">Lumen account</p><h1 className="mt-2 text-3xl font-black">{signup?"Start your journey":"Welcome back"}</h1>{signup&&<label className="mt-5 block text-sm font-bold">First name<input required className="mt-1 w-full rounded-xl border p-3" placeholder="What should we call you?"/></label>}<label className="mt-4 block text-sm font-bold">Email<input type="email" required className="mt-1 w-full rounded-xl border p-3" placeholder="you@example.com"/></label><label className="mt-4 block text-sm font-bold">Password<input type="password" required minLength={8} className="mt-1 w-full rounded-xl border p-3" placeholder="At least 8 characters"/></label>{signup&&<><label className="mt-4 block text-sm font-bold">Date of birth<input type="date" required className="mt-1 w-full rounded-xl border p-3"/></label><label className="mt-4 flex gap-2 text-xs"><input required type="checkbox"/> I agree to the Terms and Privacy Policy. I understand Lumen is for ages 13+.</label></>}<button className="mt-6 w-full rounded-xl bg-indigo-600 p-3 font-bold text-white">{signup?"Create secure account":"Sign in"}</button>{done&&<p role="status" className="mt-4 rounded-xl bg-mint p-3 text-sm">Demo complete. Connect this form to Supabase Auth or Firebase Auth before production — passwords are never stored by this app.</p>}<p className="mt-5 text-center text-sm">{signup?"Already have an account?":"New here?"} <Link className="font-bold text-indigo-600" href={signup?"/signin":"/signup"}>{signup?"Sign in":"Create one"}</Link></p></form> }
